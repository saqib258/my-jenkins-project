pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git url: 'https://github.com/saqib258/my-jenkins-project.git', branch: 'main'
            }
        }

        stage('Build') {
            steps {
                sh 'ls -la'
                echo "Build stage completed"
            }
        }
    }
}
