// ... other directives

agent any // or agent { node { label 'your-agent' } }

stages {
    stage('Build Docker Image') {
        steps {
            sh 'docker build -t hello-jenkins .' // Fails here
        }
    }
    // ...
}
