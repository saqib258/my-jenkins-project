stage('Build Docker Image') {
    agent {
        docker {
            image 'docker:dind'
            args '--privileged' // Gives necessary permissions to the dind container
        }
    }
    steps {
        sh 'docker build -t hello-jenkins .'
    }
}
