stage('Build Docker Image') {
    agent {
        docker {
            image 'docker:dind'
            args '--privileged' // Necessary for the dind image to run the daemon
        }
    }
    steps {
        sh 'docker build -t hello-jenkins .'
    }
}
